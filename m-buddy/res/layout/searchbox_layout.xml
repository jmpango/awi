<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:background="@color/search_box_bg"
    android:orientation="vertical" >
<!-- 
    <LinearLayout
        android:id="@+id/search_banner_dv"
        android:layout_width="fill_parent"
        android:layout_height="64dp"
        android:background="@drawable/banner_bg" >
        
        <LinearLayout
            android:id="@+id/search_leftsider_dv"
            android:layout_width="5dip"
            android:layout_height="fill_parent"
            android:layout_marginLeft="@dimen/margin_10" />
        
        <include layout="@layout/navigation_divider" />
        
        <ImageView
            android:id="@+id/search_quick"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|center_horizontal"
            android:layout_marginLeft="@dimen/margin_10"
            android:contentDescription="@string/default_desc"
            android:scaleType="center"
            android:src="@drawable/quick_search_ico" />

    </LinearLayout>

    <EditText
        android:id="@+id/advanced_search_box"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="4dp"
        android:layout_marginLeft="4dp"
        android:layout_marginRight="4dp"
        android:layout_marginTop="16dp"
        android:hint="@string/main_search_text_hint"
        android:inputType="text" />

    <EditText
        android:id="@+id/advanced_location_box"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="16dp"
        android:layout_marginLeft="4dp"
        android:layout_marginRight="4dp"
        android:layout_marginTop="4dp"
        android:fontFamily="sans-serif"
        android:hint="@string/main_search_location_text_hint"
        android:inputType="text" />
-->
</LinearLayout> 